import { ScreenSizeClassNameManagerDirective } from './screen-size-class-name-manager.directive';

describe('ScreenSizeContentManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenSizeClassNameManagerDirective();
    expect(directive).toBeTruthy();
  });
});
